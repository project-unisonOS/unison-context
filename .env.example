# Unison Context Service
# Feature flags and security controls

# Require consent grants for protected endpoints. When true, endpoints like /kv/get
# and /kv/put will enforce consent scopes (REPLAY_READ, INGEST_WRITE).
# Default: false (staged rollout)
UNISON_REQUIRE_CONSENT=false
